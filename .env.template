# ============================================
# CONFIGURATION FOR DISTRIBUTED PARKING SYSTEM
# ============================================
# Copy this file to .env and update IP addresses for each node

# Máy Master IP (có thể dùng cho Node 1 - Airflow hoặc quản lý)
MASTER_IP=192.168.80.84

# Node 1: Airflow + Redis (Celery)
# Nếu chạy trên máy master, dùng MASTER_IP, nếu không thì thay bằng IP của Node 1
NODE1_IP=192.168.80.84
REDIS_CELERY_PORT=6379
AIRFLOW_WEBSERVER_PORT=8080

# Node 2: Camera Producer + Kafka Broker
# Thay bằng IP thực của máy Node 2
NODE2_IP=192.168.80.85
KAFKA_BROKER_PORT=9092
KAFKA_ZOOKEEPER_PORT=2181
KAFKA_TOPIC=parking-events

# Node 3: Spark Streaming Processor
# Thay bằng IP thực của máy Node 3
NODE3_IP=192.168.80.86
SPARK_MASTER_PORT=7077
SPARK_UI_PORT=4040

# Node 4: Cassandra Database
# Thay bằng IP thực của máy Node 4
NODE4_IP=192.168.80.87
CASSANDRA_PORT=9042
CASSANDRA_CQL_PORT=7000

# Node 5: Redis (Realtime Cache)
# Thay bằng IP thực của máy Node 5
NODE5_IP=192.168.80.88
REDIS_CACHE_PORT=6379
REDIS_DB=2

# Node 6: Streamlit Dashboard
# Thay bằng IP thực của máy Node 6
NODE6_IP=192.168.80.89
DASHBOARD_PORT=8501

# Parking Fee Configuration
PRICE_PER_MINUTE=10000

# Kafka Configuration
# QUAN TRỌNG: Phải là IP thực của Node 2 (máy chạy Kafka)
KAFKA_BOOTSTRAP_SERVERS=${NODE2_IP}:9092

# Redis Configuration
# QUAN TRỌNG: Phải là IP thực của Node 5 (máy chạy Redis Cache)
REDIS_CACHE_HOST=${NODE5_IP}
REDIS_CACHE_PORT=${REDIS_CACHE_PORT}

# Cassandra Configuration
# QUAN TRỌNG: Phải là IP thực của Node 4 (máy chạy Cassandra)
CASSANDRA_HOST=${NODE4_IP}
CASSANDRA_PORT=${CASSANDRA_PORT}
